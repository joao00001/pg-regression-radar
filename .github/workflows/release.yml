name: Release

# Publishes tagged releases: the five container images this repo's Dockerfile
# builds (cli, operator, manager, collector, ingester — see the Dockerfile's
# own top comment for why one Dockerfile builds all five) to GHCR, plus the
# Helm chart (deploy/helm/deploylens) as an OCI artifact to the same
# registry, plus a GitHub Release on the pushed tag itself with notes (see
# the `release-notes` job at the bottom — it extracts the tag's own section
# from CHANGELOG.md, which the repo owner generates from per-PR fragments
# in changelog.d/ via `towncrier build` before pushing the tag; see
# CONTRIBUTING.md's "Release notes / changeset fragments" section). Every other
# Docker-related workflow in this repo (e2e-manual.yml, e2e-kind.yml) only
# ever builds images locally for its own use within a single job — this is
# the only workflow that publishes anything anywhere.
#
# Deliberately gated on pushing a `v*` tag, not "every merge to main": this
# repo has no "edge"/continuously-updated image, matching a normal tagged-
# release cadence instead of a rolling one. Push a tag (e.g. `git tag v0.2.0
# && git push origin v0.2.0`) to cut a release.
#
# ---------------------------------------------------------------------------
# Manual prerequisites (cannot be set from workflow YAML — repo owner must
# configure these once, before the first tag push):
#
#   1. Settings -> Actions -> General -> Workflow permissions: select
#      "Read and write permissions". Without this, the default GITHUB_TOKEN
#      is read-only and every `docker push`/`helm push` below fails with 403,
#      regardless of the `permissions:` block in this file (repo-level
#      settings cap what a workflow's token can be granted).
#   2. Nothing else. GHCR is free for public repositories and needs no
#      separate registry credential/secret — GITHUB_TOKEN (scoped by the
#      `permissions:` block below) is sufficient to push both the container
#      images and the Helm OCI artifact. Cosign's keyless signing likewise
#      needs no stored secret: it exchanges this workflow run's own OIDC
#      token (`id-token: write` below) for a short-lived signing certificate
#      from Sigstore's public Fulcio CA at sign time.
# ---------------------------------------------------------------------------
#
# Versioning: the pushed git tag (e.g. `v0.2.0`) becomes both the container
# image tag (alongside a floating `latest`) and the Helm chart's
# `--app-version` at package time. The chart's own `--version` (which, unlike
# `appVersion`, must be strict SemVer with no `v` prefix — see
# https://helm.sh/docs/topics/charts/#the-chartyaml-file) is derived by
# stripping the leading `v`. deploy/helm/deploylens/Chart.yaml's checked-in
# `version`/`appVersion` fields are NOT read for this — `helm package
# --version --app-version` overrides them unconditionally — so Chart.yaml's
# checked-in values are only a development-time placeholder (what you get
# packaging an unreleased checkout locally), not a value that needs bumping
# in lockstep with every tag. This avoids a whole separate PR/commit per
# release just to edit Chart.yaml, at the cost of Chart.yaml's own version
# field being a slightly misleading "last hand-edited" value rather than
# "current release" — documented in docs/ci-cd.md and docs/installation.md.
on:
  push:
    tags:
      - "v*"

env:
  # ghcr.io/joao00001/pg-regression-radar — matches the repository path
  # deploy/helm/deploylens/values.yaml already hardcodes for the operator
  # and manager images (image.repository /
  # manager.image.repository); the other three targets below just append
  # their own name the same way.
  IMAGE_BASE: ghcr.io/${{ github.repository }}
  # Floor severity that fails the job — see the "Scan" step below. Kept as
  # an env var (rather than inlined) so bumping it later is a one-line diff.
  TRIVY_SEVERITY: CRITICAL

permissions:
  contents: read

jobs:
  images:
    # Each (target, platform) pair is scanned and pushed independently as
    # its own arch-specific tag (":<version>-amd64", ":<version>-arm64") —
    # see the "Push" step below for why. The `manifests` job (needs: images)
    # combines each target's two arch-specific tags into the single
    # multi-arch ":<version>"/":latest" tags that
    # deploy/helm/deploylens/values.yaml and docs/installation.md actually
    # reference; nothing under those two tags exists until `manifests` runs.
    name: "Image: ${{ matrix.target }} (${{ matrix.platform }})"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # docker push / helm push to ghcr.io
      id-token: write # cosign keyless signing (OIDC token exchange with Fulcio)
    strategy:
      fail-fast: false
      matrix:
        # Exactly the five --target values the Dockerfile defines — see its
        # top comment. Each is also the exact path suffix under
        # ${IMAGE_BASE}/, matching values.yaml's existing operator/manager
        # image.repository convention.
        target: [cli, operator, manager, collector, ingester]
        # linux/arm64 alongside the existing linux/amd64: EDB Postgres AI
        # and CloudNativePG both run on arm64 Kubernetes nodes (Graviton on
        # EKS, Ampere/Axion on GKE/AKS, Apple Silicon `kind` clusters for
        # local dev), and this repo's own Dockerfile already cross-compiles
        # cleanly for it (see the Dockerfile's GOARCH=$TARGETARCH comment) —
        # there was no technical reason left to publish amd64-only images.
        platform: [linux/amd64, linux/arm64]

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Resolve release version and arch from the pushed tag / matrix
        # github.ref_name is already the bare tag name (e.g. "v0.2.0") with
        # no refs/tags/ prefix to strip, for a `push: tags:`-triggered run.
        # DATE is computed here (not via github.event.head_commit.timestamp)
        # because head_commit is not reliably populated on tag-push events.
        # ARCH strips the "linux/" prefix off matrix.platform (e.g.
        # "linux/arm64" -> "arm64") for use in this job's own tag/artifact
        # names below.
        run: |
          echo "VERSION=${{ github.ref_name }}" >> "$GITHUB_ENV"
          echo "DATE=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_ENV"
          echo "ARCH=${PLATFORM#linux/}" >> "$GITHUB_ENV"
        env:
          PLATFORM: ${{ matrix.platform }}

      - name: Set up QEMU
        # Registers binfmt_misc handlers so buildx *can* run non-native-arch
        # containers under emulation. The Dockerfile's build stage avoids
        # actually needing this (see its --platform=$BUILDPLATFORM comment)
        # by cross-compiling instead, but this is kept as a defensive,
        # standard part of the documented multi-arch buildx setup in case a
        # future Dockerfile change (an added RUN step in a runtime stage,
        # say) starts needing it — cheap to keep, expensive to silently miss.
        uses: docker/setup-qemu-action@v3
        with:
          platforms: ${{ matrix.platform }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (load locally, do not push yet)
        # push: false + load: true builds the image and hands it to the
        # runner's local Docker daemon under a throwaway, run-scoped tag —
        # nothing reaches ghcr.io until the Trivy scan below passes. This is
        # the same "scan before it's public" ordering as the CRITICAL-fails-
        # the-job gate itself: a build that fails the scan should never have
        # been pushed in the first place, not just flagged after the fact.
        #
        # platforms is a single value here (never a comma-separated list):
        # `load: true` only works for a single-platform build — buildx has
        # no way to "load" a multi-arch manifest list into a single-arch
        # Docker daemon. That's the whole reason this matrix has a
        # `platform` dimension building one arch-specific image per job,
        # rather than one `docker buildx build --platform linux/amd64,linux/arm64`
        # per target: a true multi-platform build can only be `push: true`,
        # which would mean pushing before Trivy ever gets to scan it.
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          target: ${{ matrix.target }}
          platforms: ${{ matrix.platform }}
          push: false
          load: true
          tags: pgrr-release-build/${{ matrix.target }}:${{ github.sha }}-${{ env.ARCH }}
          build-args: |
            VERSION=${{ env.VERSION }}
            COMMIT=${{ github.sha }}
            DATE=${{ env.DATE }}
          # No buildx-generated provenance/SBOM manifest entries: this
          # workflow generates and attaches its own SBOM via Syft + cosign
          # attest below, and provenance attestations only make sense on a
          # `push: true` build anyway (this step never pushes).
          provenance: false
          sbom: false

      - name: Generate SBOM (SPDX)
        # SPDX over CycloneDX: it's anchore/sbom-action's own default, it's
        # an ISO/IEC 5962:2021 standard, and it's the format GitHub's own
        # native dependency-graph SBOM export uses — the least surprising
        # choice for anyone consuming this later. Scans the image already
        # loaded into the local Docker daemon by the step above (no registry
        # round-trip needed, and nothing has been pushed yet regardless).
        #
        # continue-on-error here, paired with the conditional retry step
        # right below: this action downloads syft's release tarball from
        # github.com's Releases CDN at run time, and a transient
        # connection failure there (HTTP status=000 — the connection
        # never completed) hit this exact step on the real v1.0.0-rc1
        # release run. Re-running the job from the Actions UI fixed it on
        # the very next attempt with no code change, confirming a
        # flaky-network blip rather than a real config problem — one
        # automatic retry means a future release doesn't need a human to
        # notice the failure and manually re-run the job.
        id: sbom
        continue-on-error: true
        uses: anchore/sbom-action@v0
        with:
          image: pgrr-release-build/${{ matrix.target }}:${{ github.sha }}-${{ env.ARCH }}
          format: spdx-json
          output-file: sbom-${{ matrix.target }}-${{ env.ARCH }}.spdx.json
          artifact-name: sbom-${{ matrix.target }}-${{ env.ARCH }}-${{ env.VERSION }}.spdx.json

      - name: Generate SBOM (SPDX) — retry after transient failure
        # Only runs if the attempt above failed; skipped entirely when it
        # succeeded. No continue-on-error here — if this retry also
        # fails, the job fails for real, same as before this change. A
        # *persistent* SBOM failure should still block the release.
        if: steps.sbom.outcome == 'failure'
        uses: anchore/sbom-action@v0
        with:
          image: pgrr-release-build/${{ matrix.target }}:${{ github.sha }}-${{ env.ARCH }}
          format: spdx-json
          output-file: sbom-${{ matrix.target }}-${{ env.ARCH }}.spdx.json
          artifact-name: sbom-${{ matrix.target }}-${{ env.ARCH }}-${{ env.VERSION }}.spdx.json

      - name: Scan for vulnerabilities (Trivy)
        # Runs against the same not-yet-pushed local image. exit-code: '1'
        # fails this step — and therefore the job, since every step after it
        # is plain sequential (no continue-on-error) — the moment a
        # CRITICAL-or-higher finding shows up, before the "Push" step below
        # ever runs. Nothing reaches ghcr.io if this fails.
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: pgrr-release-build/${{ matrix.target }}:${{ github.sha }}-${{ env.ARCH }}
          format: table
          severity: ${{ env.TRIVY_SEVERITY }}
          exit-code: "1"

      - name: Push
        # Pushed under an arch-specific tag only (":<version>-amd64",
        # ":<version>-arm64") — never directly as ":<version>"/":latest"
        # themselves. Those multi-arch tags are created afterwards by the
        # `manifests` job, combining both arch-specific images this matrix
        # produces via `docker buildx imagetools create` (a registry-side
        # operation, no rebuild). Plain `docker tag`/`docker push` against
        # the already-built, already-scanned local image (re-tagged straight
        # from the exact image Trivy just approved) rather than a second
        # buildx invocation — no reason to rebuild (or risk building
        # something subtly different) just to change the push flag.
        id: push
        run: |
          LOCAL="pgrr-release-build/${{ matrix.target }}:${{ github.sha }}-${{ env.ARCH }}"
          IMAGE="${IMAGE_BASE}/${{ matrix.target }}"

          docker tag "$LOCAL" "${IMAGE}:${VERSION}-${ARCH}"
          docker push "${IMAGE}:${VERSION}-${ARCH}"

          DIGEST="$(docker inspect "${IMAGE}:${VERSION}-${ARCH}" --format='{{index .RepoDigests 0}}' | cut -d@ -f2)"
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Install cosign
        # sigstore/cosign-installer does not publish a floating major-version
        # tag (unlike actions/checkout@v7, docker/*-action@v4/v7, etc. above)
        # — only full semver release tags exist, so this must be pinned to a
        # complete version per the action's own README, not a "v4" shorthand.
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign the published image (keyless)
        # No KMS key, no cosign.key, no COSIGN_PASSWORD secret: `--yes`
        # skips cosign's interactive confirmation prompt, and keyless mode
        # exchanges this job's own GitHub Actions OIDC token (permissions:
        # id-token: write, above) for a short-lived signing certificate from
        # Sigstore's public Fulcio CA, then records the signature in the
        # public Rekor transparency log. Signs by digest (not by the mutable
        # arch-specific tag) so the signature is bound to the exact bytes
        # just scanned and pushed. The `manifests` job additionally signs
        # each target's combined multi-arch index digest, so both the
        # per-arch image and the tag most people will actually pull are
        # independently verifiable.
        run: cosign sign --yes "${{ steps.push.outputs.image }}@${{ steps.push.outputs.digest }}"

      - name: Attach the SBOM as a signed attestation
        # In-toto attestation, keyless-signed the same way as the image
        # signature above, attached directly to the image's digest in
        # ghcr.io — so `cosign verify-attestation` (see docs/installation.md)
        # is enough to fetch and verify it later with no separate file to
        # lose track of.
        run: cosign attest --yes --predicate "sbom-${{ matrix.target }}-${{ env.ARCH }}.spdx.json" --type spdxjson "${{ steps.push.outputs.image }}@${{ steps.push.outputs.digest }}"

  manifests:
    # Combines each target's linux/amd64 and linux/arm64 images (pushed
    # above as ":<version>-amd64"/":<version>-arm64") into the single
    # ":<version>" and ":latest" tags everything else in this repo
    # (deploy/helm/deploylens/values.yaml, docs/installation.md, the "Push"
    # step's own pre-multi-arch tags before this change) has always
    # referenced — a multi-arch manifest list/OCI image index, exactly like
    # every official multi-arch image on Docker Hub. `docker buildx
    # imagetools create` does this as a pure registry-side operation (it
    # only writes a small manifest-list document referencing the two
    # already-pushed digests) — no rebuilding, no re-pulling, no re-scanning
    # needed, since both arch-specific images it points at were already
    # scanned individually by the `images` job above.
    name: "Manifest list: ${{ matrix.target }}"
    runs-on: ubuntu-latest
    needs: images
    permissions:
      contents: read
      packages: write # docker push (imagetools create) to ghcr.io
      id-token: write # cosign keyless signing (OIDC token exchange with Fulcio)
    strategy:
      matrix:
        target: [cli, operator, manager, collector, ingester]
    steps:
      - name: Resolve release version from the pushed tag
        run: echo "VERSION=${{ github.ref_name }}" >> "$GITHUB_ENV"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Create and push the multi-arch manifest list
        id: create
        run: |
          IMAGE="${IMAGE_BASE}/${{ matrix.target }}"

          docker buildx imagetools create \
            --tag "${IMAGE}:${VERSION}" \
            --tag "${IMAGE}:latest" \
            "${IMAGE}:${VERSION}-amd64" \
            "${IMAGE}:${VERSION}-arm64"

          DIGEST="$(docker buildx imagetools inspect "${IMAGE}:${VERSION}" --format '{{json .Manifest}}' | jq -r '.digest')"
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"

      - name: Install cosign
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign the multi-arch manifest list (keyless)
        # Signs the index digest itself (in addition to the two per-arch
        # digests already signed in the `images` job above), so `cosign
        # verify` against the plain ":<version>"/":latest" tag most people
        # will actually pull — see docs/installation.md — works directly,
        # without needing to know either per-arch digest first.
        run: cosign sign --yes "${{ steps.create.outputs.image }}@${{ steps.create.outputs.digest }}"

  helm-chart:
    name: Helm chart (OCI)
    runs-on: ubuntu-latest
    needs: manifests
    permissions:
      contents: read
      packages: write # helm push to ghcr.io
      id-token: write # cosign keyless signing (OIDC token exchange with Fulcio)
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Resolve chart/app version from the pushed tag
        run: |
          echo "APP_VERSION=${{ github.ref_name }}" >> "$GITHUB_ENV"
          # Chart.yaml's `version` must be strict SemVer (no leading "v") —
          # https://helm.sh/docs/topics/charts/#the-chartyaml-file —
          # while `appVersion` is a free-form string and keeps the "v"
          # prefix to match the container image tags above and each
          # binary's own --version output (internal/buildinfo.Version).
          echo "CHART_VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_ENV"

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          # Same version e2e-kind.yml already pins, for one fewer Helm
          # version to reason about across this repo's workflows.
          version: v3.19.0

      - name: Log in to GHCR (Helm OCI)
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io --username "${{ github.actor }}" --password-stdin

      - name: Log in to GHCR (for cosign)
        # `helm registry login` above writes its credential to Helm's own
        # config file ($HELM_REGISTRY_CONFIG, defaulting to
        # ~/.config/helm/registry/config.json) — enough for `helm push`
        # below, but cosign doesn't read that file. cosign resolves registry
        # credentials through go-containerregistry's default keychain, which
        # only looks at the standard Docker config location
        # (~/.docker/config.json / $DOCKER_CONFIG). Without this second,
        # separate login, "Sign the published Helm chart" below fails with
        # "UNAUTHORIZED: unauthenticated" even though the chart push itself
        # just succeeded — this is exactly what happened on the real
        # v1.0.0 tag push, the first time this job ever ran for real (see
        # docs/roadmap.md). The `images`/`manifests` jobs above never hit
        # this because they already log in with docker/login-action before
        # their own cosign sign step; this job only had the Helm-specific
        # login until now.
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Package the chart
        # --version/--app-version override whatever is checked into
        # Chart.yaml unconditionally — see this workflow's top comment for
        # why Chart.yaml's own committed version field is deliberately not
        # bumped per release.
        run: |
          helm package deploy/helm/deploylens \
            --version "${CHART_VERSION}" \
            --app-version "${APP_VERSION}" \
            --destination /tmp/helm-out

      - name: Push the chart (OCI)
        # oci://ghcr.io/<owner>/charts is the convention the Helm project's
        # own docs and the wider Helm+GHCR ecosystem have converged on for
        # "one OCI repo per chart, grouped under a `charts` namespace",
        # keeping chart artifacts visually and path-wise separate from the
        # container image packages published above
        # (ghcr.io/joao00001/pg-regression-radar/*). Installable afterwards
        # as `oci://ghcr.io/joao00001/charts/pg-regression-radar` — see
        # docs/installation.md.
        #
        # `helm push` prints a "Digest: sha256:..." line for the artifact it
        # just pushed (the same digest `helm pull`/OCI clients resolve the
        # tag to) — captured here so the signing step below can sign by
        # digest, the same "pin the exact bytes just published" approach
        # the `images` job already uses for the container images, rather
        # than the mutable chart-version tag.
        id: push
        run: |
          helm push "/tmp/helm-out/pg-regression-radar-${CHART_VERSION}.tgz" "oci://ghcr.io/${{ github.repository_owner }}/charts" 2>&1 | tee /tmp/helm-push.log

          DIGEST="$(grep -oE 'Digest: sha256:[0-9a-f]+' /tmp/helm-push.log | awk '{print $2}')"
          if [ -z "$DIGEST" ]; then
            echo "::error::Could not parse a 'Digest: sha256:...' line out of 'helm push' output — see the log above. Cannot sign the chart without knowing its digest."
            exit 1
          fi
          echo "digest=${DIGEST}" >> "$GITHUB_OUTPUT"
          echo "ref=ghcr.io/${{ github.repository_owner }}/charts/pg-regression-radar" >> "$GITHUB_OUTPUT"

      - name: Install cosign
        # sigstore/cosign-installer does not publish a floating major-version
        # tag — see the same note on this step in the `images` job above.
        uses: sigstore/cosign-installer@v4.1.2

      - name: Sign the published Helm chart (keyless)
        # The five container images have been cosign-signed since this
        # workflow's first version; the Helm OCI artifact itself was not —
        # see docs/roadmap.md's former "Helm chart signing not implemented"
        # gap. cosign signs any OCI artifact by digest, not just container
        # images, so this is the exact same keyless flow (GitHub Actions
        # OIDC exchanged with Sigstore's public Fulcio CA, signature
        # recorded in the public Rekor transparency log) already used
        # above — no separate key-management story needed, contrary to what
        # that roadmap entry originally assumed before this was looked into.
        # See docs/installation.md's "Verify the chart's signature" section
        # for the exact `cosign verify` command this repo documents for it.
        run: cosign sign --yes "${{ steps.push.outputs.ref }}@${{ steps.push.outputs.digest }}"

  release-notes:
    name: Publish GitHub Release notes
    runs-on: ubuntu-latest
    needs: [images, manifests, helm-chart]
    # Waits on every other job so a GitHub Release is never published
    # pointing at images/manifests/chart that failed to build, scan, sign,
    # combine, or push. `images` is listed explicitly even though
    # `helm-chart`'s own `needs: manifests` (which itself needs `images`)
    # already implies it transitively — being explicit here means this
    # job's dependency graph stays correct on its own even if `helm-chart`
    # is ever restructured to depend on something other than `manifests`.
    permissions:
      contents: write # softprops/action-gh-release creates/updates the Release
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Release notes come from CHANGELOG.md, not a changelog-generation
      # tool run in this job. This repo's changelog is built by
      # `towncrier` (see towncrier.toml and CONTRIBUTING.md's "Release
      # notes / changeset fragments" section) from per-PR fragments in
      # changelog.d/: the repo owner runs `towncrier build --version
      # ${TAG} --yes` locally, which rewrites CHANGELOG.md with a new
      # "## ${TAG} - <date>" section and removes the consumed fragments,
      # commits that, and only then pushes the `v*` tag that triggers this
      # workflow — so by the time this job runs, the section for the
      # exact tag being released is already sitting at/near the top of
      # CHANGELOG.md on the tagged commit. This job's only work is
      # extracting that one section and handing it to
      # softprops/action-gh-release as the Release body.
      #
      # (An earlier version of this job used `git-cliff` to generate release
      # notes from commit messages directly, with a checked-in `cliff.toml`
      # — it briefly landed on `main` before being replaced by the
      # towncrier-based flow above: a generated-from-commit-messages
      # changelog and a generated-from-user-facing-fragments changelog are
      # redundant, and the fragment-based one is enforced in CI per-PR (see
      # .github/workflows/changelog-fragment-check.yml) while commit
      # messages are not. `cliff.toml` has been deleted; there is no
      # `git-cliff` usage left in this repo.)
      - name: Extract this tag's section from CHANGELOG.md
        id: notes
        env:
          TAG: ${{ github.ref_name }}
        run: |
          if [ ! -f CHANGELOG.md ]; then
            echo "::error::CHANGELOG.md not found at tag ${TAG}. Run 'towncrier build --version ${TAG} --yes' and commit the result (the same commit you then tag) before pushing a release tag — see CONTRIBUTING.md#release-notes--changeset-fragments."
            exit 1
          fi

          # From the line "## ${TAG} ..." (towncrier's title_format renders
          # exactly this, e.g. "## v0.5.0 - 2026-08-12") up to, but not
          # including, the next "## " heading (the previous release's
          # section) or end of file.
          awk -v tag="$TAG" '
            $0 ~ "^## " tag "([[:space:]]|$)" { found=1; print; next }
            found && /^## / { exit }
            found { print }
          ' CHANGELOG.md > /tmp/release-notes.md

          if [ ! -s /tmp/release-notes.md ]; then
            echo "::error::No '## ${TAG} ...' section found in CHANGELOG.md. 'towncrier build --version ${TAG} --yes' must be run and committed as (or before) the tagged commit — see CONTRIBUTING.md#release-notes--changeset-fragments."
            exit 1
          fi

          echo "Extracted release notes for ${TAG}:"
          cat /tmp/release-notes.md

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: ${{ github.ref_name }}
          body_path: /tmp/release-notes.md
