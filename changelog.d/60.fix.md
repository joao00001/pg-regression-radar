Add a separate `docker/login-action` step to the release workflow's Helm chart job, so `cosign` can find registry credentials it needs to sign the chart — `helm registry login` alone writes to a config file `cosign` never reads, which made the chart-signing step fail with an authentication error on every release.
