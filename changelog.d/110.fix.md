The Helm chart Secret template now includes the `pg-regression-radar.io/allow-postgreswatch-access: "true"` consent label, fixing out-of-the-box breakage where the controller rejected the chart-created Secret on every reconciliation loop.
