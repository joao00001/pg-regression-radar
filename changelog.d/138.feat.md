Introduces the `PostgresRadarCluster` CRD so administrators can pre-register remote clusters; `PostgresWatch` now references them by name via `spec.remoteClusterRef`, eliminating the confused-deputy risk of direct kubeconfig Secret access.
