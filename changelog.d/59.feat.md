Fleet mode now evicts a remote cluster's cached client immediately after it fails a real request, so a transient network blip or a genuinely rotated kubeconfig no longer keeps a broken connection in circulation until a future reconcile happens to notice.
