Wired `internal/telemetry/promsource` into the CRD-driven `cmd/manager` path: `PostgresWatch.spec.sampleSource.type: prometheus` now selects it as an opt-in alternative to `internal/collector.Collector`, skipping `dsn`/`dsnSecretRef` entirely and rejecting `capturePlans: true` (which needs a direct database connection this mode doesn't have) with a clear error instead of ignoring it.
