Added a Helm render test (`deploy/helm/deploylens/tests/security-context.sh`) that runs `helm template` for both `operator` and `manager` modes and asserts all required security-context fields are present and set to the expected values (`runAsNonRoot`, `runAsUser`/`runAsGroup`, `seccompProfile: RuntimeDefault`, `allowPrivilegeEscalation: false`, `readOnlyRootFilesystem: true`, `capabilities.drop: [ALL]`), and that no container mounts arbitrary writable temp paths by default.
