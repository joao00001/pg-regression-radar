Add `spec.capturePlans` to `PostgresWatch`, so the CRD-driven `manager` mode can attach a plan-diff summary to a detected `PerformanceRegression`'s `status.planDiffSummary`, matching the standalone `operator` CLI's existing `--capture-plans` behavior.
