Add `spec.remoteNamespace` to `PostgresWatch`, letting a fleet's remote-cluster DSN Secret live in a differently-named namespace than the hub-side `PostgresWatch`, instead of requiring matching namespace names on both sides.
