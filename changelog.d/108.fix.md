Replaced bare `http.ListenAndServe` in the collector metrics server with a timeout-aware `*http.Server` (setting `ReadHeaderTimeout`, `ReadTimeout`, `WriteTimeout`, and `IdleTimeout`) and added graceful shutdown on context cancellation, resolving gosec G114 / CWE-676. Also added the same timeout fields to `httpRunnable` in the operator manager, resolving the companion G112 / CWE-400 finding.
