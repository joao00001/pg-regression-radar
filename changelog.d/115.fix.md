Added the `pg-regression-radar.io/allow-postgreswatch-access: "true"` consent label to the Helm chart's Secret template so a default `mode: manager` install works without manual post-install patching.
